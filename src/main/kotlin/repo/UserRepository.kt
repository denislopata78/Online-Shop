package repo

import DataBase
import models.User

class UserRepository {
    fun addItemToCart(user: User) {
    }
    fun removeItemFromCart(user: User) {
    }
    fun updateCartItems(user: User) {
    }
    fun addUser(user: User) {
    }
    fun removeUser(user: User) {
    }
    fun getUser(user: User) {
    }
}
