package repo

import models.Product
import models.User

class UserRepo : ArrayList<User>() {
    fun addUser(user: User) {
    }
    fun alterUser(user: User) {
    }
    fun deleteUser(user: User) {
    }
    fun getUserByLogin(user: User) {
    }
}
