package repo

import models.Product
class ProductRepo : ArrayList<Product>() {
    fun addProduct(product: Product) {
    }
    fun alterProduct(product: Product) {
    }
    fun deleteProduct(id: Int) {
    }
}
