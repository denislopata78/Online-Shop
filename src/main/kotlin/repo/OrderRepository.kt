package repo

import models.Order
import models.User

class OrderRepository {
    fun addNewOrder(order: Order) {
    }
    fun getUserOrders(user: User) {
    }
    fun changeOrderStatus(order: Order) {
    }
}